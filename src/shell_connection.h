#ifndef _NCOT_SHELL_CONNECTION_H_
#define _NCOT_SHELL_CONNECTION_H_

#include "context.h"
#include "connection.h"

void ncot_shell_connection_list(struct ncot_context *context, struct ncot_connection *connection);

#endif
